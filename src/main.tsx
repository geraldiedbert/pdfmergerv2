import React from 'react';
import ReactDOM from 'react-dom/client';
import PDFMergerApp from './PDFMergerApp';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PDFMergerApp />
  </React.StrictMode>
);
